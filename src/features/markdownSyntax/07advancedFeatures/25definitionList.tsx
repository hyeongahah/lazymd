import { List } from 'lucide-react';
import { ToolbarButton } from '@/components/Toolbar/ToolbarButton';

export function DefinitionListButton() {
  return (
    <ToolbarButton onClick={() => {}} title='Definition List'>
      <List size={18} />
    </ToolbarButton>
  );
}
